def line(people)
  phrase = "The line is currently: "
  if people.length == 0
    puts "The line is currently empty."
  else 
    people.each do |name|
    phrase.push(name.index += 1. )
    phrase.push(name)
    end
  end
end
